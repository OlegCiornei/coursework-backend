#flyway strategy
spring.jpa.hibernate.ddl-auto=update

#base path
server.servlet.context-path=/api

#security
jwt.header=Authorization
jwt.secret=LatulTheBest
jwt.expiration=3600

#logging
current.date=
logging.file.name=target/logs/working-${current.date}.log

spring.profiles.default=local